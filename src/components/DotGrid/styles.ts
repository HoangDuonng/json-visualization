import styled from "styled-components";

export const StyledDotGrid = styled.section`
  position: absolute;
  inset: 0;
  overflow: hidden;
  pointer-events: none;
`;

export const StyledWrap = styled.div`
  width: 100%;
  height: 100%;
  position: relative;
`;

export const StyledCanvas = styled.canvas`
  display: block;
  width: 100%;
  height: 100%;
  pointer-events: auto;
`;
